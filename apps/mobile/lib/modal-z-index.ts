/** Bottom sheets / action sheets — below confirmation dialogs. */
export const MODAL_Z_INDEX_ACTION_SHEET = 40;

/** Confirmation and alert dialogs — always above sheets. */
export const MODAL_Z_INDEX_DIALOG = 50;
