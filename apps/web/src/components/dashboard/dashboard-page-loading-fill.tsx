/** Hält den Planungs-Content-Bereich während Dashboard-RSC-Load gefüllt (kein Brand-Backdrop-Flash). */
export function DashboardPageLoadingFill() {
  return <div className="flex min-h-0 flex-1 flex-col" aria-hidden />;
}
