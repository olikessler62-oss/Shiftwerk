/**
 * Einstellungs-Modale auf der aktuellen Seite (Dashboard, Planung, …) statt immer /dashboard.
 * Auf `false` setzen oder SETTINGS_MODALS_ROLLBACK.md — altes Verhalten (nur Dashboard-Hintergrund).
 */
export const SETTINGS_MODALS_ON_CURRENT_PAGE = true;
